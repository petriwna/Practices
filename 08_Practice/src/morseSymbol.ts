export class MorseSymbol {
    symbol: string;
    translation: string;
    constructor(symbol: string, translation: string) {
        this.symbol = symbol;
        this.translation = translation;
    }
}